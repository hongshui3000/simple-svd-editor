import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import { action } from '@storybook/addon-actions';
import Modal from '.';

<Meta title="Controls / Modal" component={Modal} />

# Modal

<Story
    name="default"
    args={{
        theme: 'dark',
        icon: true,
        title: 'Название сообщения',
        message: 'Текст сообщения',
        closeBtn: true,
        onClose: action('close'),
    }}
    argTypes={{
        theme: {
            options: ['white', 'dark', 'error', 'warning', 'success'],
            control: { type: 'select' },
        },
    }}
>
    {args => <Modal {...args} />}
</Story>

<ArgsTable story="default" />

<Canvas>
    <Story name="white">
        <Modal theme="white" icon message="Текст сообщения" title="Название сообщения" />
    </Story>
</Canvas>

<Canvas>
    <Story name="success">
        <Modal theme="dark" icon message="Текст сообщения" title="Название сообщения" />
    </Story>
</Canvas>

<Canvas>
    <Story name="warning">
        <Modal theme="warning" icon message="Текст сообщения" title="Название сообщения" />
    </Story>
</Canvas>

<Canvas>
    <Story name="error">
        <Modal theme="error" icon message="Текст сообщения" title="Название сообщения" />
    </Story>
</Canvas>
