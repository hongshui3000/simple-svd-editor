import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import DropdownButton from './index';

<Meta title="Controls / Deprecated/ DropdownButton" component={DropdownButton} />

# DropdownButton

DEPRECATED
DO NOT USE THIS COMPONENT
USE Tooltip component as dropdown

<Story
    name="Controls"
    args={{
        buttonContent: 'Документы',
        isDropOpen: false,
        isDropRight: false,
        isCloseOnClick: true,
        isDisabled: false,
    }}
    argTypes={{
        buttonContent: { control: 'text' },
        isDropOpen: { control: 'boolean' },
        isDropRight: { control: 'boolean' },
        isCloseOnClick: { control: 'boolean' },
        isDisabled: { control: 'boolean' },
    }}
>
    {args => (
        <DropdownButton {...args}>
            <DropdownButton.Option onClick={() => console.log('Карточка сборки')}>
                Карточка сборки
            </DropdownButton.Option>
            <DropdownButton.Option>Опись</DropdownButton.Option>
            <DropdownButton.Option>Акт приёма-передачи</DropdownButton.Option>
        </DropdownButton>
    )}
</Story>

## Docs

Кнопка с выпадашкой. Используется вне формы.
Каждый пункт меню представляет собой тег `<button>`, который обёрнут в компонент `<DropdownButton.Option>`.
Основная активирующая кнопка является компонентом `<Button>` из пакета `gds`.

## Props

<ArgsTable story="DropdownButton" />

## Use cases

### Basic

<Canvas>
    <Story name="Basic">
        <div style={{ height: '100px' }}>
            <DropdownButton buttonContent="Мои документы">
                <DropdownButton.Option onClick={() => console.log('меня нажали')}>
                    Карточка сборки
                </DropdownButton.Option>
                <DropdownButton.Option>Опись</DropdownButton.Option>
                <DropdownButton.Option onClick={() => console.log('ай')}>Акт приёма-передачи</DropdownButton.Option>
            </DropdownButton>
        </div>
    </Story>
</Canvas>
